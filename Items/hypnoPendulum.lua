-- local sprite_cone = Sprite.new("trafficConeSpr", path.combine(PATH, "Sprites/item/trafficCone.png"), 1, 17, 17)

-- local cone = Item.new("trafficCone", NAMESPACE)
-- cone:set_sprite(sprite_cone)
-- cone:set_tier(ItemTier.RARE)
-- cone.loot_tags = Item.LootTag.CATEGORY_UTILITY

-- local log = ItemLog.new_from_item(cone)
-- log.group = ItemLog.Group.RARE

-- local coneObj = Object.new()

-- Callback.add(Callback.ON_STEP, function()

-- for _, actor in ipairs(solar:get_holding_actors()) do

	-- if actor.still_timer >= 2 *60.0 then
		
	-- end
-- end

-- end)